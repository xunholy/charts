apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enemy-territory.fullname" . }}-test-connection"
  labels:
    {{- include "enemy-territory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enemy-territory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
